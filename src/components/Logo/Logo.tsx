import React from "react";
import styles from "./style.module.scss";

const Logo = () => {
  return (
    <div className={styles.logo_wrapper}>
      <a href="https://korabli.su/"></a>
    </div>
  );
};

export default Logo;
